This patch was retired in C4dll-R 1.7.

The wrapper again keeps a normal output/window at least as large as the logical
game canvas. build.ps1 deliberately does not apply this file; it remains only as
a tombstone because this workspace's ACL does not permit deleting the tracked
file in place.
