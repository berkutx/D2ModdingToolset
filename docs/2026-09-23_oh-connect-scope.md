# Область исправления предыгрового Abort

- Авторизация: пользователь попросил исправить повторно пойманный отказ ОХ и
  включить исправление в PR10 и PR11.
- В области: MSS, чтение локального EXE и сохранённой диагностики, узкий фикс,
  регрессионные тесты, сборка и обычная публикация в обе существующие ветки.
- Вне области: изменение EXE, серверных данных и рейтинга, отключение проверок
  игровых команд, установка новой DLL без отдельного запроса.
- Сеть: существующий GitHub remote проекта; анализ бинарника через локальную IDA.
- Логи двух клиентов содержат приватные данные и не публикуются. В отчёте только
  необходимые метаданные первого отказа, без payload, ников, токенов и адресов.
